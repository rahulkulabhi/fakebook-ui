import { ContentWrapPipe } from './content-wrap.pipe';

describe('ContentWrapPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentWrapPipe();
    expect(pipe).toBeTruthy();
  });
});
